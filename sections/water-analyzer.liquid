<!-- Analyseur Eau Potable - Version avec Formulaire Natif Shopify -->
<div class="water-analyzer" data-section-id="{{ section.id }}" style="
  --primary-color: {{ section.settings.primary_color | default: '#667eea' }};
  --secondary-color: {{ section.settings.secondary_color | default: '#764ba2' }};
  --accent-color: {{ section.settings.accent_color | default: '#f093fb' }};
  --button-color: {{ section.settings.button_color | default: '#ff6b6b' }};
  --text-color: {{ section.settings.text_color | default: '#333333' }};
  --border-radius: {{ section.settings.border_radius | default: 20 }}px;
  --section-padding: {{ section.settings.section_padding | default: 2 }}rem;
">
  <!-- Header -->
  <header class="water-analyzer__header">
    <h2 class="water-analyzer__title">
      <span class="water-analyzer__icon">{{ section.settings.header_icon | default: '🌊' }}</span>
      {{ section.settings.title | default: 'Analyseur Qualite Eau Potable' }}
    </h2>
    <p class="water-analyzer__subtitle">
      {{ section.settings.subtitle | default: 'Decouvrez la qualite de l eau a votre adresse' }}
    </p>
  </header>

  <!-- Main Content -->
  <div class="water-analyzer__content">
    
    <!-- Formulaire Natif Shopify -->
    <section class="water-analyzer__form-section">
      {% form 'contact', class: 'water-analyzer__native-form' %}
        
        <!-- Messages de retour Shopify -->
        {% if form.posted_successfully? %}
          <div class="water-analyzer__success-message">
            <h3>✅ Demande envoyée avec succès !</h3>
            <p>Nous avons bien reçu votre demande d'analyse pour <strong>{{ form.address }}</strong></p>
            <p>Vous recevrez les résultats par email sous 24h.</p>
          </div>
        {% endif %}

        {% if form.errors %}
          <div class="water-analyzer__error-message">
            <h4>❌ Erreur dans le formulaire</h4>
            {% for field in form.errors %}
              <p>{{ field | capitalize }} : {{ form.errors[field] }}</p>
            {% endfor %}
          </div>
        {% endif %}

        <!-- Section de recherche -->
        <div class="water-analyzer__search">
          <h3 class="water-analyzer__search-title">
            <span class="water-analyzer__icon">{{ section.settings.search_icon | default: '🏠' }}</span>
            {{ section.settings.search_title | default: 'Analysez votre eau' }}
          </h3>
          <p class="water-analyzer__search-description">
            {{ section.settings.search_description | default: 'Saisissez votre adresse pour obtenir un rapport de qualite detaille' }}
          </p>

          <!-- Champs du formulaire -->
          <div class="water-analyzer__form-fields">
            
            <!-- Adresse (obligatoire) -->
            <div class="water-analyzer__field-group">
              <label for="contact_address" class="water-analyzer__label">
                🏠 Votre adresse *
              </label>
              <input 
                type="text" 
                id="contact_address"
                name="contact[address]"
                class="water-analyzer__input"
                placeholder="{{ section.settings.placeholder | default: '01000 Bourg-en-Bresse' }}"
                required
                value="{{ form.address }}"
              >
            </div>

            <!-- Email (obligatoire) -->
            <div class="water-analyzer__field-group">
              <label for="contact_email" class="water-analyzer__label">
                📧 Votre email *
              </label>
              <input 
                type="email" 
                id="contact_email"
                name="contact[email]"
                class="water-analyzer__input"
                placeholder="votre@email.com"
                required
                value="{{ form.email }}"
              >
            </div>

            <!-- Nom (optionnel) -->
            {% if section.settings.collect_name %}
            <div class="water-analyzer__field-group">
              <label for="contact_name" class="water-analyzer__label">
                👤 Votre nom
              </label>
              <input 
                type="text" 
                id="contact_name"
                name="contact[name]"
                class="water-analyzer__input"
                placeholder="Jean Dupont"
                value="{{ form.name }}"
              >
            </div>
            {% endif %}

            <!-- Téléphone (optionnel) -->
            {% if section.settings.collect_phone %}
            <div class="water-analyzer__field-group">
              <label for="contact_phone" class="water-analyzer__label">
                📞 Votre téléphone
              </label>
              <input 
                type="tel" 
                id="contact_phone"
                name="contact[phone]"
                class="water-analyzer__input"
                placeholder="06 12 34 56 78"
                value="{{ form.phone }}"
              >
            </div>
            {% endif %}

            <!-- Type d'analyse -->
            {% if section.settings.analysis_types != blank %}
            <div class="water-analyzer__field-group">
              <label for="contact_analysis_type" class="water-analyzer__label">
                🔬 Type d'analyse souhaité
              </label>
              <select 
                id="contact_analysis_type"
                name="contact[analysis_type]"
                class="water-analyzer__select"
              >
                <option value="">Sélectionnez un type</option>
                {% assign types = section.settings.analysis_types | split: ',' %}
                {% for type in types %}
                  <option value="{{ type | strip }}" {% if form.analysis_type == type %}selected{% endif %}>
                    {{ type | strip }}
                  </option>
                {% endfor %}
              </select>
            </div>
            {% endif %}

            <!-- Message personnalisé -->
            <div class="water-analyzer__field-group">
              <label for="contact_body" class="water-analyzer__label">
                💬 Message (optionnel)
              </label>
              <textarea 
                id="contact_body"
                name="contact[body]"
                class="water-analyzer__textarea"
                placeholder="Précisez vos besoins ou questions..."
                rows="3"
              >{{ form.body }}</textarea>
            </div>

            <!-- Consentement RGPD -->
            {% if section.settings.require_consent %}
            <div class="water-analyzer__field-group water-analyzer__consent">
              <label class="water-analyzer__checkbox-label">
                <input 
                  type="checkbox" 
                  name="contact[consent]"
                  value="true"
                  required
                  class="water-analyzer__checkbox"
                >
                <span class="water-analyzer__checkmark"></span>
                J'accepte que mes données soient utilisées pour cette analyse et les recommandations associées
              </label>
            </div>
            {% endif %}

            <!-- Newsletter (optionnel) -->
            {% if section.settings.newsletter_optin %}
            <div class="water-analyzer__field-group water-analyzer__newsletter">
              <label class="water-analyzer__checkbox-label">
                <input 
                  type="checkbox" 
                  name="contact[newsletter]"
                  value="true"
                  class="water-analyzer__checkbox"
                >
                <span class="water-analyzer__checkmark"></span>
                Je souhaite recevoir des conseils et offres sur la qualité de l'eau
              </label>
            </div>
            {% endif %}

          </div>

          <!-- Bouton de soumission -->
          <div class="water-analyzer__submit-section">
            <button 
              type="submit"
              class="water-analyzer__submit-button"
            >
              <span class="water-analyzer__button-icon">{{ section.settings.button_icon | default: '🔍' }}</span>
              <span class="water-analyzer__button-text">{% assign default_text = "Demander l'analyse" %}{{ section.settings.button_text | default: default_text }}</span>
            </button>
          </div>

          <!-- Exemples d'adresses -->
          {% if section.settings.show_examples %}
          <div class="water-analyzer__examples">
            <span class="water-analyzer__examples-label">💡 Exemples :</span>
            {% assign examples = section.settings.examples | default: '01000,Gex,Bourg-en-Bresse' | split: ',' %}
            {% for example in examples %}
              <button 
                type="button" 
                class="water-analyzer__example"
                data-example="{{ example | strip }}"
              >
                {{ example | strip }}
              </button>
            {% endfor %}
          </div>
          {% endif %}

        </div>

        <!-- Champs cachés pour le traitement -->
        <input type="hidden" name="contact[tags]" value="water-analysis,{{ section.settings.contact_tags | default: 'lead' }}">
        <input type="hidden" name="contact[subject]" value="Demande d'analyse eau - {{ section.settings.title }}">
        
      {% endform %}
    </section>

    <!-- Section d'analyse instantanée (optionnelle) -->
    {% if section.settings.enable_instant_analysis %}
    <section class="water-analyzer__instant-section">
      <div class="water-analyzer__instant-toggle">
        <h3>⚡ Analyse instantanée</h3>
        <p>Obtenez un aperçu immédiat de la qualité de l'eau dans votre région</p>
        <button type="button" class="water-analyzer__instant-button" data-instant-analysis>
          Analyse rapide
        </button>
      </div>

      <!-- Résultats de l'analyse instantanée -->
      <div class="water-analyzer__instant-results" data-instant-results style="display: none;">
        <!-- Contenu généré par JavaScript -->
      </div>
    </section>
    {% endif %}

    <!-- Informations sur le processus -->
    <section class="water-analyzer__process-info">
      <h3>🔬 Notre processus d'analyse</h3>
      <div class="water-analyzer__process-steps">
        <div class="water-analyzer__step">
          <div class="water-analyzer__step-number">1</div>
          <div class="water-analyzer__step-content">
            <h4>Réception de votre demande</h4>
            <p>Nous analysons votre adresse et identifions les sources d'eau locales</p>
          </div>
        </div>
        <div class="water-analyzer__step">
          <div class="water-analyzer__step-number">2</div>
          <div class="water-analyzer__step-content">
            <h4>Analyse des données officielles</h4>
            <p>Consultation des bases de données gouvernementales sur la qualité de l'eau</p>
          </div>
        </div>
        <div class="water-analyzer__step">
          <div class="water-analyzer__step-number">3</div>
          <div class="water-analyzer__step-content">
            <h4>Recommandations personnalisées</h4>
            <p>Suggestions de produits adaptés à votre situation spécifique</p>
          </div>
        </div>
      </div>
    </section>

  </div>
</div>

<style>
  .water-analyzer {
    font-family: var(--font-body-family, 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif);
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
    border-radius: var(--border-radius);
    overflow: hidden;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
    margin: var(--section-padding) 0;
    color: var(--text-color);
    max-width: 100%;
    position: relative;
  }

  .water-analyzer__header {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    color: white;
    padding: var(--section-padding) calc(var(--section-padding) * 0.75);
    text-align: center;
  }

  .water-analyzer__title {
    font-size: clamp(1.5rem, 4vw, 2rem);
    margin: 0 0 0.5rem 0;
    font-weight: 700;
    line-height: 1.2;
  }

  .water-analyzer__subtitle {
    margin: 0;
    opacity: 0.9;
    font-size: clamp(0.9rem, 2.5vw, 1.1rem);
  }

  .water-analyzer__icon {
    display: inline-block;
    margin-right: 0.5rem;
  }

  .water-analyzer__content {
    background: white;
    padding: var(--section-padding) calc(var(--section-padding) * 0.75);
  }

  /* Messages de retour */
  .water-analyzer__success-message {
    background: linear-gradient(135deg, #a8e6cf 0%, #dcedc1 100%);
    padding: 1.5rem;
    border-radius: calc(var(--border-radius) * 0.75);
    margin-bottom: var(--section-padding);
    text-align: center;
  }

  .water-analyzer__success-message h3 {
    margin: 0 0 1rem 0;
    color: #2d5a27;
  }

  .water-analyzer__error-message {
    background: #f8d7da;
    color: #721c24;
    padding: 1.5rem;
    border-radius: calc(var(--border-radius) * 0.75);
    margin-bottom: var(--section-padding);
    border-left: 4px solid #dc3545;
  }

  .water-analyzer__error-message h4 {
    margin: 0 0 1rem 0;
  }

  /* Section de recherche */
  .water-analyzer__search {
    background: linear-gradient(135deg, var(--accent-color) 0%, #f5576c 100%);
    padding: var(--section-padding);
    border-radius: calc(var(--border-radius) * 0.75);
    margin-bottom: var(--section-padding);
    color: white;
  }

  .water-analyzer__search-title {
    margin: 0 0 1rem 0;
    font-size: 1.3rem;
    font-weight: 600;
  }

  .water-analyzer__search-description {
    margin: 0 0 1.5rem 0;
    opacity: 0.95;
  }

  /* Champs de formulaire */
  .water-analyzer__form-fields {
    display: grid;
    gap: 1.25rem;
    margin-bottom: 1.5rem;
  }

  .water-analyzer__field-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .water-analyzer__label {
    font-weight: 600;
    font-size: 0.95rem;
    opacity: 0.95;
  }

  .water-analyzer__input,
  .water-analyzer__select,
  .water-analyzer__textarea {
    padding: 0.875rem 1.25rem;
    border: none;
    border-radius: 25px;
    font-size: 1rem;
    background: rgba(255, 255, 255, 0.95);
    outline: none;
    transition: all 0.3s ease;
    font-family: inherit;
  }

  .water-analyzer__input:focus,
  .water-analyzer__select:focus,
  .water-analyzer__textarea:focus {
    background: white;
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.3);
  }

  .water-analyzer__textarea {
    border-radius: 15px;
    resize: vertical;
    min-height: 80px;
  }

  .water-analyzer__select {
    cursor: pointer;
  }

  /* Cases à cocher */
  .water-analyzer__consent,
  .water-analyzer__newsletter {
    margin-top: 0.5rem;
  }

  .water-analyzer__checkbox-label {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    cursor: pointer;
    font-size: 0.9rem;
    line-height: 1.4;
  }

  .water-analyzer__checkbox {
    appearance: none;
    width: 20px;
    height: 20px;
    border: 2px solid rgba(255, 255, 255, 0.7);
    border-radius: 4px;
    background: rgba(255, 255, 255, 0.1);
    cursor: pointer;
    position: relative;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .water-analyzer__checkbox:checked {
    background: rgba(255, 255, 255, 0.9);
    border-color: rgba(255, 255, 255, 0.9);
  }

  .water-analyzer__checkbox:checked::after {
    content: '✓';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--accent-color);
    font-weight: bold;
    font-size: 14px;
  }

  /* Bouton de soumission */
  .water-analyzer__submit-section {
    text-align: center;
    margin-bottom: 1.5rem;
  }

  .water-analyzer__submit-button {
    padding: 1rem 2rem;
    background: linear-gradient(45deg, var(--button-color), #ee5a52);
    color: white;
    border: none;
    border-radius: 30px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    min-width: 200px;
    justify-content: center;
  }

  .water-analyzer__submit-button:hover:not(:disabled) {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(238, 85, 82, 0.4);
  }

  .water-analyzer__submit-button:disabled {
    opacity: 0.7;
    cursor: not-allowed;
    transform: none;
  }

  /* Exemples */
  .water-analyzer__examples {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    opacity: 0.9;
    font-size: 0.9rem;
    margin-top: 1rem;
  }

  .water-analyzer__examples-label {
    font-weight: 600;
    margin-right: 0.5rem;
  }

  .water-analyzer__example {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    border: none;
    padding: 0.375rem 0.75rem;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s ease;
    font-size: 0.85rem;
  }

  .water-analyzer__example:hover {
    background: rgba(255, 255, 255, 0.3);
    transform: translateY(-1px);
  }

  /* Section d'analyse instantanée */
  .water-analyzer__instant-section {
    background: #f8f9fa;
    padding: 1.5rem;
    border-radius: calc(var(--border-radius) * 0.75);
    margin-bottom: var(--section-padding);
    text-align: center;
  }

  .water-analyzer__instant-button {
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    cursor: pointer;
    font-weight: 600;
    transition: all 0.3s ease;
    margin-top: 1rem;
  }

  .water-analyzer__instant-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
  }

  /* Informations sur le processus */
  .water-analyzer__process-info {
    background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
    padding: var(--section-padding);
    border-radius: calc(var(--border-radius) * 0.75);
  }

  .water-analyzer__process-info h3 {
    margin: 0 0 1.5rem 0;
    text-align: center;
    color: #8b4513;
  }

  .water-analyzer__process-steps {
    display: grid;
    gap: 1.5rem;
  }

  .water-analyzer__step {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
  }

  .water-analyzer__step-number {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    flex-shrink: 0;
  }

  .water-analyzer__step-content h4 {
    margin: 0 0 0.5rem 0;
    color: #8b4513;
  }

  .water-analyzer__step-content p {
    margin: 0;
    color: #a0522d;
    line-height: 1.5;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .water-analyzer {
      margin: 1rem 0;
      border-radius: calc(var(--border-radius) * 0.75);
    }

    .water-analyzer__header {
      padding: 1.5rem 1rem;
    }

    .water-analyzer__content {
      padding: 1.5rem 1rem;
    }

    .water-analyzer__search {
      padding: 1.5rem;
    }

    .water-analyzer__examples {
      justify-content: center;
    }

    .water-analyzer__submit-button {
      width: 100%;
    }
  }

  @media (max-width: 480px) {
    .water-analyzer__examples {
      flex-direction: column;
      align-items: flex-start;
    }

    .water-analyzer__examples-label {
      margin-bottom: 0.5rem;
    }

    .water-analyzer__step {
      flex-direction: column;
      text-align: center;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .water-analyzer {
      border: 2px solid #000;
    }

    .water-analyzer__submit-button {
      border: 2px solid #000;
    }
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .water-analyzer__submit-button:hover {
      transform: none;
    }

    .water-analyzer__example:hover {
      transform: none;
    }
  }
</style>

<script>
(function() {
  'use strict';
  
  // Configuration
  const CONFIG = {
    sectionId: '{{ section.id }}',
    enableInstantAnalysis: {{ section.settings.enable_instant_analysis | default: false }}
  };

  // Utilitaires
  function setExample(address) {
    const input = document.querySelector('#contact_address');
    if (input) {
      input.value = address;
      input.focus();
    }
  }

  // Analyse instantanée (optionnelle)
  function initInstantAnalysis() {
    if (!CONFIG.enableInstantAnalysis) return;

    const button = document.querySelector('[data-instant-analysis]');
    const results = document.querySelector('[data-instant-results]');

    if (button) {
      button.addEventListener('click', function() {
        const address = document.querySelector('#contact_address')?.value;
        if (!address) {
          alert('Veuillez d\'abord saisir une adresse');
          return;
        }

        // Ici on pourrait intégrer le code d'analyse existant
        results.innerHTML = '<p>🔍 Analyse en cours pour: ' + address + '</p>';
        results.style.display = 'block';
      });
    }
  }

  // Gestionnaire des exemples
  function initExamples() {
    const examples = document.querySelectorAll('[data-example]');
    examples.forEach(function(example) {
      example.addEventListener('click', function() {
        const address = this.dataset.example;
        setExample(address);
        
        // Animation de feedback
        this.style.transform = 'scale(0.95)';
        setTimeout(() => {
          this.style.transform = '';
        }, 150);
      });
    });
  }

  // Validation du formulaire
  function initFormValidation() {
    const form = document.querySelector('.water-analyzer__native-form');
    if (!form) return;

    form.addEventListener('submit', function(e) {
      const email = form.querySelector('[name="contact[email]"]')?.value;
      const address = form.querySelector('[name="contact[address]"]')?.value;
      const consent = form.querySelector('[name="contact[consent]"]');

      if (!email || !address) {
        e.preventDefault();
        alert('Veuillez remplir tous les champs obligatoires');
        return;
      }

      if (consent && !consent.checked) {
        e.preventDefault();
        alert('Veuillez accepter l\'utilisation de vos données');
        return;
      }

      // Ajouter des données supplémentaires
      const hiddenData = document.createElement('input');
      hiddenData.type = 'hidden';
      hiddenData.name = 'contact[submission_date]';
      hiddenData.value = new Date().toISOString();
      form.appendChild(hiddenData);
    });
  }

  // Initialisation
  function init() {
    console.log('🌊 Water Analyzer (Native Form) initialized');
    initExamples();
    initFormValidation();
    initInstantAnalysis();
  }

  // Démarrage
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }

})();
</script>

{% schema %}
{
  "name": "Analyseur Eau",
  "settings": [
    {
      "type": "header",
      "content": "Configuration générale"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Titre principal",
      "default": "Analyseur Qualité Eau Potable",
      "info": "Le titre affiché en haut de la section"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "Découvrez la qualité de l'eau à votre adresse",
      "info": "Description affichée sous le titre"
    },
    {
      "type": "text",
      "id": "search_title",
      "label": "Titre de la section recherche",
      "default": "Analysez votre eau",
      "info": "Titre de la zone de recherche"
    },
    {
      "type": "textarea",
      "id": "search_description",
      "label": "Description de la recherche",
      "default": "Saisissez votre adresse pour obtenir un rapport de qualité détaillé",
      "info": "Texte explicatif pour la recherche"
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Placeholder du champ adresse",
      "default": "01000 Bourg-en-Bresse",
      "info": "Texte d'exemple dans le champ de saisie"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Texte du bouton",
      "default": "Demander l'analyse",
      "info": "Texte affiché sur le bouton de soumission"
    },
    {
      "type": "header",
      "content": "Champs du formulaire"
    },
    {
      "type": "checkbox",
      "id": "collect_name",
      "label": "Collecter le nom",
      "default": true,
      "info": "Ajouter un champ nom au formulaire"
    },
    {
      "type": "checkbox",
      "id": "collect_phone",
      "label": "Collecter le téléphone",
      "default": false,
      "info": "Ajouter un champ téléphone au formulaire"
    },
    {
      "type": "text",
      "id": "analysis_types",
      "label": "Types d'analyse",
      "default": "Analyse complète,Analyse bactériologique,Analyse chimique,Test de dureté",
      "info": "Types d'analyse proposés (séparés par des virgules)"
    },
    {
      "type": "checkbox",
      "id": "require_consent",
      "label": "Consentement RGPD obligatoire",
      "default": true,
      "info": "Exiger l'acceptation du traitement des données"
    },
    {
      "type": "checkbox",
      "id": "newsletter_optin",
      "label": "Option newsletter",
      "default": true,
      "info": "Proposer l'inscription à la newsletter"
    },
    {
      "type": "header",
      "content": "Fonctionnalités"
    },
    {
      "type": "checkbox",
      "id": "show_examples",
      "label": "Afficher les exemples",
      "default": true,
      "info": "Affiche les boutons d'exemples d'adresses"
    },
    {
      "type": "text",
      "id": "examples",
      "label": "Exemples d'adresses",
      "default": "01000,Gex,Bourg-en-Bresse",
      "info": "Liste d'exemples séparés par des virgules"
    },
    {
      "type": "checkbox",
      "id": "enable_instant_analysis",
      "label": "Activer l'analyse instantanée",
      "default": false,
      "info": "Permet une analyse rapide en plus du formulaire"
    },
    {
      "type": "header",
      "content": "Icônes et emojis"
    },
    {
      "type": "text",
      "id": "header_icon",
      "label": "Icône du header",
      "default": "🌊",
      "info": "Emoji affiché dans le titre principal"
    },
    {
      "type": "text",
      "id": "search_icon",
      "label": "Icône de recherche",
      "default": "🏠",
      "info": "Emoji affiché dans le titre de recherche"
    },
    {
      "type": "text",
      "id": "button_icon",
      "label": "Icône du bouton",
      "default": "🔍",
      "info": "Emoji affiché dans le bouton"
    },
    {
      "type": "header",
      "content": "Couleurs et design"
    },
    {
      "type": "color",
      "id": "primary_color",
      "label": "Couleur primaire",
      "default": "#667eea",
      "info": "Couleur principale du gradient de fond"
    },
    {
      "type": "color",
      "id": "secondary_color",
      "label": "Couleur secondaire",
      "default": "#764ba2",
      "info": "Couleur secondaire du gradient de fond"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Couleur d'accent",
      "default": "#f093fb",
      "info": "Couleur de la zone de recherche"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Couleur du bouton",
      "default": "#ff6b6b",
      "info": "Couleur du bouton d'analyse"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Couleur du texte",
      "default": "#333333",
      "info": "Couleur du texte principal"
    },
    {
      "type": "range",
      "id": "border_radius",
      "label": "Arrondi des bords",
      "min": 0,
      "max": 50,
      "step": 5,
      "default": 20,
      "unit": "px",
      "info": "Arrondi des coins de la section"
    },
    {
      "type": "range",
      "id": "section_padding",
      "label": "Espacement interne",
      "min": 1,
      "max": 5,
      "step": 1,
      "default": 2,
      "unit": "rem",
      "info": "Espacement interne de la section"
    },
    {
      "type": "header",
      "content": "Configuration technique"
    },
    {
      "type": "text",
      "id": "contact_tags",
      "label": "Tags de contact",
      "default": "lead,water-analysis",
      "info": "Tags ajoutés aux contacts (séparés par des virgules)"
    }
  ],
  "presets": [
    {
      "name": "Analyseur Eau"
    }
  ]
}
{% endschema %}